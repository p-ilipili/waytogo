# waytogo
Paris route optimization for tourists based on given coordinates

It calculates the fastest route from point A to point B using the metro. 
For points A and B, all metro sations are listed in a radius of e.g. 600m (modify the radius parameter).

Then all optimal roads are calculated between both points, using all paths between all metros. 

The output is still to improve.

